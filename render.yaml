services:
  - type: web
    name: resilience360-backend
    env: node
    plan: free
    branch: main
    buildCommand: npm install
    startCommand: npm run server
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: OPENAI_API_KEY
        sync: false
